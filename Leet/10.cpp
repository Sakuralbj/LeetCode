#include<iostream>
#include<string>
using namespace std;
class Solution {
public:
    bool isMatch(string s, string p) {
       if(s.length()==0&&p.length()==0){
           return true;
       }
       if(s.length()==0){
           if((s.length()==1&&p[0]=='*')||(s.length()==2&&p[1]=='*')){
               retur


       }
       int s1=s.length();
       int p1=p.length();
       
               

        
    }
};